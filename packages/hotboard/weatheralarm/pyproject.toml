[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hotboard-weatheralarm"
version = "0.1.0"
description = "中央气象台气象预警"
requires-python = ">=3.12"
dependencies = [
    "hotboard-core>=0.1.0",
    "typer",
]
authors = [
    {name = "llnancy"}
]
license = {text = "Apache-2.0"}


[project.urls]
Homepage = "https://github.com/hotboard/hotboard"
Repository = "https://github.com/hotboard/hotboard"
Issues = "https://github.com/hotboard/hotboard/issues"

[project.scripts]
hotboard-weatheralarm = "hotboard.weatheralarm.main:run"

[tool.setuptools.packages.find]
where = ["."]
include = ["hotboard.weatheralarm*"]

[tool.setuptools.package-dir]
"hotboard.weatheralarm" = "."
